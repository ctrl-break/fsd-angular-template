{
    "$schema": "http://json-schema.org/schema",
    "ID": "fsd-feature-schema",
    "title": "FSD feature schema",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "feature name",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        },
        "path": {
            "type": "string",
            "description": "Path for generation"
        }
    },
    "required": ["name"]
}
