{
    "$schema": "http://json-schema.org/schema",
    "ID": "fsd-page-schema",
    "title": "FSD page schema",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "page name",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        },
        "path": {
            "type": "string",
            "description": "Path for generation"
        }
    },
    "required": ["name"]
}
