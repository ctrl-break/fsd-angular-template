{
    "$schema": "http://json-schema.org/schema",
    "ID": "fsd-entity-schema",
    "title": "FSD Entity Schema",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Entity name",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        },
        "path": {
            "type": "string",
            "description": "Path for generation"
        }
    },
    "required": ["name"]
}
